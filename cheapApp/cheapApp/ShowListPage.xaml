<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="cheapApp.ShowListPage"
             Title="Your Lists">
  <StackLayout>
    <Grid 
      VerticalOptions="FillAndExpand"
			HorizontalOptions="FillAndExpand">
      <Grid.RowDefinitions>
        <RowDefinition/>
        <RowDefinition/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition
					Width="*" />
      </Grid.ColumnDefinitions>
    </Grid>

    <Button x:Name="Add" Text="Add" Grid.Row="1" Grid.Column="0" BackgroundColor="Green" TextColor="White"
              Clicked="AddPage"
              />
    
  </StackLayout>
</ContentPage>